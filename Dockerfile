# Вказуємо базовий образ
FROM node:20.15.0

# Встановлюємо робочу директорію в контейнері
WORKDIR /app

# Копіюємо package.json та package-lock.json до робочої директорії
COPY package*.json ./

# Встановлюємо залежності
RUN npm install

# Копіюємо решту файлів проекту до робочої директорії (не обов'язково, якщо використовуєте томи)
# COPY . .

# Вказуємо порт, на якому буде працювати додаток
EXPOSE 3000

# Запускаємо додаток у режимі розробки
CMD ["npm", "run", "develop"]
